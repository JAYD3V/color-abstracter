const rgbPatt = {
    RGB: /^#([A-F\d]{3}|[A-F\d]{6})$/i
    // RGB   : REGEX,
    // RGB6  : REGEX,
    // RGB3  : REGEX,
    // RGBA4 : REGEX,
    // RGBA8 : REGEX,
    // RGBA  : REGEX
};


export default rgbPatt;
